#include <iostream>
#include "functions.h"
#include <fstream>
#include <sstream>
#include <string>

int main() {

    //1
    //for (auto currentNumber = 0; currentNumber != 127; ++currentNumber) {
    //    std::cout << currentNumber << "- has " << countBits(currentNumber) << " 1 bit" << std::endl;
    //}

    //2
    //int seven = 7, order = 1; //111
    //setBit(seven,order);

    //3
    //double arrayOfRealNumbers[] = {12, 76, 23, 9, 5};
    //int five = 5;
    //std::cout << getArrayOfRealNumbersAverage(arrayOfRealNumbers, five) << std::endl;

    //4
    //std::cout << getStandardDeviationOfRealNumbers(arrayOfRealNumbers, five) << std::endl;

    //5
    //std::cout << getTwoHighestNumberFromArrayOfRealNumbers(arrayOfRealNumbers, five) << std::endl;

    //6
    //std::string lowercaseString = "";
    //std::cout << "Please, enter string: ";
    //std::getline (std::cin, lowercaseString);
    //std::cout << capitalizeWords(lowercaseString) << "!\n";

    //7
    //std::string niceString = "Hello visitor, it's me Pongi!";
    //std::cout << increaseAlphabetChar(niceString);
    //std::cout << decreaseAlphabetChar(niceString);

    //8
    //std::ifstream inputFile("inputfile.csv");
    //for( std::string line; getline( inputFile, line ); )
    //{
    //    std::vector<std::string> currentLineVector = splitCSVLine(line);
    //    std::cout << currentLineVector.front() << "," << currentLineVector.back() << std::endl;
    //}

    return 0;
}
